Structure  of the GRAPH:

Start -> Retriever which calls vectorstore and gets response -> Generator gives out output backed by LLM and retrieved data -> END

Another type includes the ReAct agent structure at the generator node